class Solution {
public:
    bool doesValidArrayExist(vector<int>& derived) {
        int output = 0;
        for (int& num : derived) {
            output ^= num;
        }
        return !output;
    }
};
